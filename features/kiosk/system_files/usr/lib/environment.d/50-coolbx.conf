# Coolbx OS: runtime-launchers (kiosk-apps uit de config-repo) leven onder /var/lib/coolbx/share,
# want /usr is read-only op bootc. Zichtbaar voor GNOME via XDG_DATA_DIRS (systemd --user).
XDG_DATA_DIRS=/var/lib/coolbx/share:${XDG_DATA_DIRS:-/usr/local/share:/usr/share}
